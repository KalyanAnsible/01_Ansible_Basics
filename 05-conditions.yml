- name: Conditions
  hosts: localhost
  become: yes
  tasks:
    - name: Check user exit or not
      ansible.builtin.command: id kalyan
      register: OUTPUT
      ignore_errors: true

    - name: print 
      ansible.builtin.debug:
        msg: "output of above task is  {{ OUTPUT }}"

    - name: print 
      ansible.builtin.debug:
        msg: "Hello kalyan"